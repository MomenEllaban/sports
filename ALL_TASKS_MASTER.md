# السجل الشامل للمهام وخارطة الطريق (ALL TASKS MASTER)
**Sports Champions ERP — متجر ومعدات الإسكندرية**
> تم التجميع والدمج والترتيب بتاريخ: 22 سبتمبر 2026.
> هذا الملف هو المرجع الشامل والوحيد لكافة المهام المكتملة، الجارية، والجديدة في كامل المشروع، مدمجاً من (`تاسكات-اليوم.md`، `NEXT_TASK.md`، `TASKS.md`، `ANALYSIS_AND_ROADMAP.md`، `SYSTEM_AUDIT_AND_GAPS_REPORT.md`، `docs/progress/PROGRESS.md`).

---

## 1. ملخص المشروع والبيئة التشغيلية
- **الريبو:** `https://github.com/MomenEllaban/sports.git` (فرع `main`).
- **الموقع الحي:** `https://sports-livid-eight.vercel.app` (فريق `momenellabans-projects` / مشروع `sports`).
- **البيئة التقنية:** Next.js 15 (App Router) + React 19 + TypeScript + Prisma ORM + Neon Serverless PostgreSQL + NextAuth v4 + Zustand + next-intl + Tailwind CSS v4 + Cloudinary.
- **قنوات البيع:**
  1. المتجر الإلكتروني B2C (`/[locale]/`) مع سلة وتتبع وبوابات دفع.
  2. نقطة البيع السريعة للكاشير (`/[locale]/pos`) على أجهزة التابلت والكمبيوتر.
  3. طلبات واتساب اليدوية ولوحة الإدارة المركزية (`/[locale]/admin`).

---

## 2. المهام المنجزة بالكامل (Completed Tasks) ✅

| الرمز | المهمة | التفاصيل المنجزة |
|---|---|---|
| **T-AUTH** | تسريع تسجيل الدخول وقاعدة البيانات | الاتصال بـ `DIRECT_URL` الخاص بـ Neon مما قلل زمن تسجيل الدخول من 700ms إلى ~140ms. |
| **T-I18N** | زر تبديل اللغة (ar/en) | دمج `LocaleSwitcher` في شريط الإدارة والـ POS والمتجر بكفاءة RTL/LTR. |
| **T-CLEAN** | تنظيف بيانات الديمو وتأمين السجل | حذف المبيعات التجريبية، وتأمين المفاتيح ضد فحص GitHub Secret Scanning. |
| **T-RMA-A** | خدمة المرتجعات والاستبدال الموحدة | بناء نموذج `ReturnRequest` + `ReturnService` وحساب فرق السعر بدقة مع الـ ETA. |
| **T-RMA-UI** | واجهات المرتجعات للعميل والأدمن | بوابة إرجاع العميل (`ReturnPortal`) بصفحة التتبع + واجهة إدارة المرتجعات وتحديث الداشبورد. |
| **T-INV-1** | طباعة الباركود والجرد الفعلي | توليد باركود EAN-13 مع واجهة الجرد (Stocktake) وحساب الفروقات آلياً. |
| **T-PAY** | بوابات الدفع Paymob + Fawry | ربط دورة Auth/Order/PaymentKey/Iframe لـ Paymob مع أرقام فوري ومسار fallback في غياب المفاتيح. |
| **T-SHIP** | تكامل الشحن Bosta + Mylerz | إنشاء بوالص الشحن وتتبعها مع مسار تراجع يدوي عند تعطل الخدمة. |
| **T-DISC** | كوبونات وعروض ونقاط ولاء | نظام موحد للخصم بموافقة PIN للمدير، استهلاك الكوبونات، واستبدال النقاط. |
| **T-VAR** | عائلات المنتجات والمقاسات | تجميع الأصناف بـ `groupSlug` وجدول مقاسات Size Chart ديناميكي. |
| **T-REV** | تقييمات العملاء وقائمة الرغبات | إمكانية تقييم المنتجات للعملاء بعد مراجعة الإدارة + قائمة رغبات للزوار. |
| **T-REP** | تقارير متقدمة وتصدير CSV | تقارير الربحية، أداء الكاشير، الشحن، والراكد وتنزيل ملفات CSV. |
| **T-DASH** | لوحة تحكم بمؤشرات KPI وشارت | رسم بياني للإيرادات، طابور طلبات المرتجعات، وإحصائيات فورية. |

---

## 3. المهام الجارية وتحت التنفيذ الفوري (In Progress) ⏳

| الرمز | المهمة | الإجراء الجاري تنفيذه |
|---|---|---|
| **T-SHIFTS** | **إصلاح وتشغيل نظام الورديات (Shifts) بالكامل** | - حل مشكلة منع فتح الوردية عند عدم اختيار الفرع في الـ POS للمدير العام.<br>- إضافة اختيار الفرع المباشر داخل واجهة الوردية.<br>- تطوير صفحة الإدارة `/admin/shifts` لتتيح فحص الدرج، الفروقات، وإغلاق الورديات إدارياً. |
| **T-POS-TAB** | **إعادة تصميم واجهة الـ POS لتناسب التابلت (Tablet Touch UI/UX)** | - القضاء على الزحام الشديد في عمود السلة والخصومات.<br>- نقل الدفع (الكوبونات، الولاء، فئات الكاش، طرق الدفع) إلى نافذة دفع مخصصة وفسيحة.<br>- تكبير أزرار اللمس (≥ 44-48px) وشبكة منتجات واضحة وسريعة. |
| **T-FEAT-LN** | **تحديث صفحة المميزات (`/features`)** | ربط كل وحدة وميزة (12 وحدة) برابط مباشر وزر تشغيلي ينقل المستخدم لصفحتها في النظام. |
| **T-QA-ALL** | **الفحص الشامل وتأكيد كافة الصفحات والأزرار** | تشغيل اختبارات الوحدة، فحص الأنواع (tsc)، التأكد من سلامة البناء (build)، واختبار الوظائف. |

---

## 4. الميزات والتحسينات الجديدة المطلوبة (New Features & Enhancements) 📋

| الرمز | الميزة الجديدة | الوصف والأثر التشغيلي |
|---|---|---|
| **NEW-01** | **إيصال كاشير حراري 80mm/58mm للـ POS** | تصميم إيصال متوافق مع الطابعات الحرارية (Thermal ESC/POS) يحتوي تفاصيل الفاتورة، الباركود، الضريبة، وبيانات الفرع مع خيار الطباعة الفورية بعد إتمام البيع. |
| **NEW-02** | **طباعة تقرير إقفال الوردية (Z-Report)** | تمكين الكاشير والإدارة من طباعة تقرير تقفيل الدرج (Z-Report) يوضح النقدية الافتتاحية، مبيعات الكاش، المرتجعات، العجز/الزيادة، وملاحظات الاستلام. |
| **NEW-03** | **حفظ طابور فواتير الـ Offline محلياً (Storage Persistence)** | حفظ فواتير الأوفلاين في الـ `localStorage` أو `IndexedDB` لحمايتها من الضياع عند عمل Refresh للمتصفح على التابلت قبل عودة النت. |
| **NEW-04** | **ربط استبدال نقاط الولاء بلمسة واحدة في الـ POS** | إظهار رصيد نقاط العميل فور كتابة رقم هاتفه في الـ POS مع زر مباشر "استبدال النقاط" يخصم القيمة المسموحة تلقائياً. |
| **NEW-05** | **تأكيد هوية المدير في عمليات الحذف (Inline Verification)** | استبدال أي دوال تنبيه `alert()` متبقية بمربعات حوار حديثة وسلسة من مكتبة `ConfirmDialog`. |

---

## 5. خطة الفجوات المتبقية على المدى المتوسط (Medium/Low Gaps)

1. **إضافة مفاتيح الربط الحقيقية في الإنتاج عند توافرها**:
   - `PAYMOB_API_KEY` و `FAWRY_MERCHANT_CODE`
   - `BOSTA_API_KEY` و `MYLERZ_KEY`
   - `ETA_SIGNING_URL`
2. **سجل تدقيق شامل (Audit Trail)**: ربط `writeAudit` بكافة عمليات تعديل الأسعار، المرتجعات، وإغلاق الورديات.
3. **ترقية تسلسل أرقام الفواتير**: منع تصادم أرقام الفواتير تحت التزامن العالي.
