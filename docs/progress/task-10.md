# Task 10 — Float to Decimal (DONE)

## RED ثم GREEN
- RED: تحويل الـ schema كسر tsc في 13 ملفاً (Decimal لا يقبل الحساب المباشر) — متوقع وموثق.
- GREEN: كل المواقع حُولت + 47/47 + build + smoke.

## ما تم
- Migration `5_money_decimal`: كل أعمدة النقود (28) إلى `DECIMAL(12,2)` — عبر `db push` + ملف + resolve (الـ direct host لا يزال P1001 أحياناً). `commissionRate` بقي Float (نسبة لا مبلغ).
- `pricing.num()`: قاعدة الحد — Decimal→number عند الخروج (JSON/client/حساب). لا حساب مباشر على Decimal أبداً.
- حُولت: مسارات البيع/الأوردرات/المنتجات/التتبع/المرتبات + صفحات (محاسبة، داشبورد، تقارير، منتجات، طلبات، مشتريات، مرتبات، موظفين، رئيسية، كتالوج).
- الكتابة تقبل number (يُحوَّل تلقائياً). القراءة المقارنة في الاختبارات عبر `Number()`.

## ملاحظات Float -> Decimal
- القيم القديمة حُفظت بدقة كاملة (تحويل in-place).
- JSON يُرجع أرقاماً (لا سلاسل) في كل الردود.
- `commissionRate` و`stockQuantity` والكميات أعداد صحيحة/نسب — خارج النطاق عمداً.
