# Task 08 — Order state machine + restock (DONE)

## RED ثم GREEN
- RED الحقيقي تعثر (خطأ syntax مكرر const كسر التحويل) — سُجل بصدق. الضمان السلوكي: اختبار الإلغاء المتوازي `[200, 400]` يفشل حتماً مع أي تنفيذ naive (read-then-write يعطي `[200, 200]` + خصم مضاعف).
- GREEN: `order-status` (5/5).

## ما تم
- `src/lib/orders/status.ts`: خريطة التحولات الموثقة + `canTransition` + `transitionOrder`. الإلغاء/المرتجع: claim شرطي + restock في **معاملة واحدة** (القفل هو صف الحالة؛ التوازي يفوز واحد فقط، والكراش يتراجع كله). لوجات RETURN بالمرجع + الفاعل، للفرع الصحيح، مرة واحدة فقط.
- `PATCH orders/[id]`: الحالة عبر الآلة + الدفع منفصل + 404/400 واضحة.
- اختبارات: إلغاء يرد المخزون، تكرار مرفوض بلا مضاعفة، توازي (فائز واحد)، انتقال عكسي مرفوض، DELIVERED لا يرد إلا عبر RETURNED.

## ملاحظات
- الطلبات القديمة (قبل T07) التي خُصمت بـ clamp قد تُعيد أكثر مما خُصم — T37 يبني تاريخاً نظيفاً، والـ checker (T11) سيرصد الفروق.
