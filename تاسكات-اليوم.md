# تاسكات اليوم — استكمال نظام المرتجعات (T-RMA)
**الحالة:** المرحلة A + الدومين + الخدمة + POS API/UI مرفوعة. الباقي أدناه بالترتيب.

## 1) إصلاحات سريعة (قبل أي شيء)
- [ ] تشغيل `typecheck` + `lint` + `build` وإصلاح أي خطأ (آخر فحص اتقطع قبل ما يخلص).
- [ ] تشغيل `rbac-matrix` meta-test (routes جديدة: returns/lookups لازم تتسجل).
- [ ] مراجعة `pos/products` route بعد إضافة `q` (تأكد إن فلتر البحث ما كسرش تحميل الـ POS العادي).

## 2) Admin UI المتبقي
- [ ] اختبار صفحة `/admin/returns` و`/[id]` و`/new` يدوياً (فلاتر، استلام، retry، manual).
- [ ] قسم المرتجعات في صفحة الطلب (returnStatus + روابط + زر) — الكود اتكتب، محتاج مراجعة بصرية.
- [ ] تحويل wizard الاسترداد القديم في `OrdersManager` (T10) لواجهة فوق المسار الجديد أو إزالته بعد التأكد.

## 3) Portal UI (متبناش لسه)
- [ ] زر "طلب مرتجع" في صفحة الطلب/التتبع للطلبات المؤهلة + سبب واضح لغير المؤهلة.
- [ ] Wizard 3 خطوات (أصناف/سبب ← صور + طريقة ← مراجعة) + صفحة حالة RTN بـ Timeline.
- [ ] APIs جاهزة (`/api/returns/request` + `/track`) — باقي الواجهة فقط.

## 4) تقارير وداشبورد
- [ ] كارت "مرتجعات تحتاج إجراء" + معدل المرتجعات في الداشبورد.
- [ ] تقرير مرتجعات (منتج/سبب/فرع/كاشير) مع إبراز `SIZE_ISSUE`.
- [ ] خصم المرتجعات من الإيراد/الربحية في `reports/summary` + تحديث `check:invariants`.

## 5) Seed واختبارات
- [ ] seed مرتجعات بكل الحالات + استبدال بفرق سعر + FAILED/MANUAL_REQUIRED.
- [ ] اختبار E2E (POS wizard + portal + `/admin/returns` على الـ viewports).
- [ ] تشغيل `test:int` كاملاً بعد سنكرمة الـ test DB (محتاج `db push` بعد migrations 18/19).

## 6) تنظيف وترحيل (مهم قبل الإنتاج)
- [ ] تشغيل `backfillLegacy()` على قاعدة الإنتاج (تحويل RETURNED القديمة لـ LEGACY بدون تحريك مخزون).
- [ ] تحويل صفوف `RefundRequest` القديمة (T10) ثم migration 20 لحذف الجدول القديم.
- [ ] تحديث `docs/ETA_GAP.md` + `docs/RETURNS.md` (التسليم النهائي) + `FINAL_REPORT` ملحق.
- [ ] إضافة `eta:retry` لملاحقة `PENDING_ETA` الخاصة بالمرتجعات.

## 7) يدوي بعد المفاتيح
- [ ] اختبار refund حقيقي sandbox (Paymob/Fawry) + مراجعة المحاسب لـ credit note.
